options:
  preview: "{{ page.frontendUrl }}"
columns:
  main:
    width: 2/3
    sections:
      pages:
        type: pages
        search: true
        sortable: true
        headline:
          en: Data Entries
          nl: Datapunten
