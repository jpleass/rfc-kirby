title: Data
options:
  preview: "{{ page.frontendUrl }}"
columns:
  main:
    width: 2/3
    sections:
      pages:
        type: pages
        create: false
        headline:
          en: Data Groups
          nl: Datagroepen
